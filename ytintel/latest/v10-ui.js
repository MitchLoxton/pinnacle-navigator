(function(){
const VERSION='0.10.1', RELEASE_KEY='ytintel-whats-new-0.10.1', PATTERN_STORE='ytintel-v10-pattern-history';
const css=`
.v10-note{margin:10px 0;padding:11px 13px;border:1px solid #303846;border-radius:12px;background:#0b0e14;color:#b9c4d5;font-size:12px;line-height:1.5}.v10-note strong{color:#fff}.v10-warn{border-color:rgba(255,196,92,.35);background:rgba(255,196,92,.055);color:#e9d49d}.v10-good{border-color:rgba(85,226,157,.3);background:rgba(85,226,157,.05)}
.takeaway-list{display:grid;gap:7px}.takeaway-row{display:grid;grid-template-columns:34px 1fr auto;gap:10px;align-items:start;padding:11px 12px;background:#0a0d12;border:1px solid #262d38;border-radius:11px}.takeaway-row .n{width:28px;height:28px;display:grid;place-items:center;border-radius:9px;background:#181d27;color:#9fb4ff;font-weight:900}.takeaway-row b{font-size:14px;line-height:1.45}.approx{font-size:10px;color:#8b98ac;white-space:nowrap}.hook-confidence{display:flex;gap:7px;flex-wrap:wrap;margin:8px 0 12px}.truth-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:9px}.truth-box{padding:13px;border:1px solid #2b3240;border-radius:13px;background:#0a0d12}.truth-box .eyebrow{margin-bottom:5px}.truth-box b{display:block;font-size:16px}.truth-box p{font-size:12px;color:#9eabba;line-height:1.45}.pattern-history{margin-top:18px}.pattern-saved{display:grid;grid-template-columns:1fr auto;gap:10px;padding:12px;border:1px solid #29303b;border-radius:12px;margin:8px 0;background:#0b0e13}.pattern-saved h4{margin:0 0 4px}.pattern-saved small{color:#8793a7}.audio-road{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.audio-card{padding:16px;border:1px solid #29303b;border-radius:14px;background:#0a0d12}.audio-card h3{margin:5px 0 8px}.audio-card .state{font-size:10px;font-weight:900;letter-spacing:.08em;color:#ffca6d}.semantic-badge{display:inline-flex;gap:6px;align-items:center;padding:5px 8px;border-radius:999px;background:rgba(141,169,255,.08);border:1px solid rgba(141,169,255,.25);font-size:10px;color:#cbd6ff;font-weight:800}.suppressed{color:#ffcd75!important}.pattern-section{margin-top:10px}.compact-source{font-size:11px;color:#8e9aad;margin:4px 0 10px;padding-left:10px;border-left:2px solid #303849}.v10-release{border-left:3px solid #55e29d!important}.v10-release .date{font-size:10px;color:#8d98aa;margin-top:2px}@media(max-width:760px){.truth-grid,.audio-road{grid-template-columns:1fr}.takeaway-row{grid-template-columns:30px 1fr}.takeaway-row .approx{grid-column:2}.pattern-saved{grid-template-columns:1fr}}
`;
const st=document.createElement('style');st.textContent=css;document.head.appendChild(st);
document.title='YTIntel v0.10.1 — Accuracy Reset';
const status=document.querySelector('#status');if(status)status.textContent='v0.10.1 ready';

// Update the permanent changelog without depending on a new HTML build.
const updates=document.querySelector('#updates .grid');
if(updates&&!updates.querySelector('.v10-release')){
 const a=document.createElement('article');a.className='card c12 release current v10-release';a.innerHTML=`<div class="split"><div><div class="eyebrow">V0.10.1 · CURRENT</div><h2 style="margin:4px 0">Accuracy reset — stop printing fake precision</h2><div class="date">4 Sep 2026</div></div><span class="pill good">LIVE</span></div><ul class="list"><li><b>30-second artefact removed from the product claims:</b> re-hook cadence, payoff cadence and “exact” deep-link timings are suppressed until cue-level captions exist.</li><li><b>WPM independently verified:</b> deduped transcript words ÷ full video minutes. The UI now says Video WPM, not speaking-only WPM.</li><li><b>Key Details → Key Takeaways:</b> sentence-level transcript evidence instead of numbered 30-second paragraphs.</li><li><b>Hook analysis is conservative:</b> high-precision hook patterns only. Ambiguous openings say they need hook-bank/model review instead of forcing a label.</li><li><b>Pattern Mine accuracy reset:</b> semantic embeddings replace shared-noun clustering. If semantic analysis fails, YTIntel refuses to fall back to noun matching.</li><li><b>Broken extreme outliers are flagged/suppressed</b> instead of confidently showing numbers such as 391×.</li><li><b>Pattern Mine saves into History.</b></li><li><b>Visual/Audio tab split into Cuts, Sound Effects and B-roll/Background Music</b> with honest LIVE/PLANNED states.</li><li><b>AI Automation / Education</b> added to niche classification.</li></ul><div class="v10-note v10-warn"><strong>Not solved yet:</strong> there is still no Claude/OpenAI-class reasoning model configured in the production backend, and the public transcript source is still coarse. v0.10 deliberately shows less rather than inventing precision.</div>`;
 updates.prepend(a);
 updates.querySelectorAll('.release.current:not(.v10-release)').forEach(x=>x.classList.remove('current'));
}

// Refresh What's New content and show once for v0.10.1.
const modal=document.querySelector('#updateModal');
if(modal){const panel=modal.querySelector('.update-panel');if(panel){panel.innerHTML=`<div class="update-top"><div class="grow"><span class="update-version">YTIntel v0.10.1 · 4 SEP 2026</span><h2>Accuracy reset.</h2><p>Claude's audit was right: the old analyser confused 30-second transcript buckets with real video behaviour. This release removes that fake precision.</p></div><button class="update-close" id="v10Close">×</button></div><div class="update-items"><div class="update-item"><div class="tick">✓</div><div><b>Fake cadence removed</b><span class="muted">No more “re-hook every 30s” just because captions arrive in 30-second blocks.</span></div></div><div class="update-item"><div class="tick">✓</div><div><b>WPM verified</b><span class="muted">Video WPM is independently recalculated from deduped words and runtime.</span></div></div><div class="update-item"><div class="tick">✓</div><div><b>Key takeaways, not numbered transcript</b><span class="muted">The report now prioritises concise sentence-level takeaways.</span></div></div><div class="update-item"><div class="tick">✓</div><div><b>Pattern Mine uses semantics</b><span class="muted">Meaning-aware embeddings replace shared-noun matching.</span></div></div><div class="update-item"><div class="tick">✓</div><div><b>Honest precision</b><span class="muted">Approximate timestamps show ≈. Exact cue-level deep links stay disabled until we truly have exact captions.</span></div></div></div><div class="update-actions"><button class="primary" id="v10Continue">Continue</button><button id="v10Log">Full update log</button></div>`;}
 const close=()=>{modal.classList.remove('show');try{localStorage.setItem(RELEASE_KEY,'seen')}catch{}};
 const open=()=>modal.classList.add('show');
 document.querySelector('#whatsNewBtn')?.addEventListener('click',open);
 document.querySelector('#v10Close')?.addEventListener('click',close);
 document.querySelector('#v10Continue')?.addEventListener('click',close);
 document.querySelector('#v10Log')?.addEventListener('click',()=>{close();if(typeof tab==='function')tab('updates')});
 try{if(localStorage.getItem(RELEASE_KEY)!=='seen')setTimeout(open,250)}catch{setTimeout(open,250)}
}

// Niche classification: more specific categories before generic gaming/YouTube terms.
if(typeof nicheFor==='function'){
 nicheFor=function(r){const q=r.research_intelligence||{},s=`${r.video?.title||''} ${r.video?.channel||''} ${(q.recurring_terms||[]).slice(0,18).map(x=>x.term).join(' ')} ${String(r.video?.description||'').slice(0,800)}`.toLowerCase();
  if(/youtube automation|faceless (?:youtube|channel)|youtube channel automation/.test(s))return'YouTube Automation';
  if(/\b(ai automation|ai education|artificial intelligence|claude|chatgpt|openai|anthropic|gemini|llm|large language model|n8n|make\.com|ai agent|agentic|prompt engineering|automated workflow|automation agency)\b/.test(s))return'AI Automation / Education';
  if(/roblox|99 nights|brookhaven|blox/.test(s))return'Roblox';if(/\bgta\b|grand theft auto|rockstar/.test(s))return'GTA / Rockstar';if(/dropship|shopify|ecommerce|e-commerce|tiktok shop|product research/.test(s))return'Organic Dropshipping';if(/minecraft|fortnite|gaming|gameplay/.test(s))return'Gaming';if(/trading|futures|crypto|stock market|forex/.test(s))return'Trading';if(/fitness|gym|workout|bodybuilding/.test(s))return'Fitness';return'Other';
 };
}

// Replace old summary n-grams with the same evidence-grounded takeaway source used above.
if(typeof methodSummaryHtml==='function'){
 methodSummaryHtml=function(r){const c=r.content_extraction||{},items=c.items||[];return `<article class="card c12 summary-card"><div class="eyebrow">QUICK TAKEAWAY</div><h2 style="margin:5px 0">What matters from this video</h2>${items.length?`<div class="takeaway-list">${items.slice(0,8).map((x,i)=>`<div class="takeaway-row"><span class="n">${i+1}</span><b>${esc(x.statement)}</b><span class="approx">${esc(x.timestamp||'')}</span></div>`).join('')}</div>`:'<p class="muted">No clean takeaway passed the evidence threshold.</p>'}<div class="v10-note"><strong>Source rule:</strong> transcript evidence only. The old three-word n-gram “method names” are disabled.</div></article>`};
}
if(typeof detailListHtml==='function'){
 detailListHtml=function(r){const c=r.content_extraction||{},items=c.items||[];if(!items.length)return'';const target=c.target_count&&c.target_count<=100?c.target_count:null,shown=items.slice(0,target||12);return `<article class="card c12"><div class="section-head"><div><div class="eyebrow">${target?`${target} KEY TAKEAWAYS`:'KEY TAKEAWAYS'}</div><h2>${target?`The useful points — stripped of transcript clutter`:'The useful points from the transcript'}</h2></div><span class="pill">sentence evidence</span></div><div class="takeaway-list">${shown.map((x,i)=>`<div class="takeaway-row"><span class="n">${i+1}</span><b>${esc(x.statement)}</b><span class="approx">${esc(x.timestamp||'')}</span></div>`).join('')}</div><div class="v10-note v10-warn"><strong>Timing:</strong> ≈ means estimated inside a coarse caption window. YTIntel is not calling this exact until cue-level JSON3 is solved.</div></article>`};
}
if(typeof hookHtml==='function'){
 hookHtml=function(r){const h=r.hook_framework||{},hs=r.hook_story||{},seq=h.sequence||[];return `<article class="card c12 hook-focus"><div class="eyebrow">HOOK ANALYSIS</div><h2 style="margin:5px 0">${esc(h.primary_type||'Unclassified')}</h2><div class="hook-confidence"><span class="pill">confidence: ${esc(h.confidence||'unknown')}</span><span class="pill">${esc(h.method||'')}</span></div><blockquote class="hook-quote">“${esc(h.exact_line||'-')}”</blockquote>${seq[0]?.why?`<p class="hook-why">${esc(seq[0].why)}</p>`:''}${seq.length>1?`<div class="hook-sequence">${seq.slice(1).map((x,i)=>`<div class="hook-move"><span>${i+2}</span><div><b>${esc(x.type)}</b><p>“${esc(x.line)}”</p><small>${esc(x.why)}</small></div></div>`).join('')}</div>`:''}<div class="hook-grid compact-hook"><div class="evidence-card"><div class="eyebrow">RE-HOOK EXAMPLE · ${esc(hs.rehook?.timestamp||'not confidently detected')}</div><p class="evidence-text">${esc(hs.rehook?.excerpt||'YTIntel did not print a re-hook because no high-confidence reset phrase was found.')}</p></div><div class="evidence-card"><div class="eyebrow">PROMISE / PAYOFF EXAMPLE · ${esc(hs.fulfillment?.timestamp||'not confidently detected')}</div><p class="evidence-text">${esc(hs.fulfillment?.excerpt||'YTIntel did not print a payoff because no clean later answer/verdict passed the rule.')}</p></div></div><div class="v10-note v10-warn"><strong>Hook bank:</strong> this classifier is intentionally conservative and is not a Claude-class model yet. Your labelled hook bank will become the evaluation/few-shot layer for the paid reasoning model.</div></article>`};
}

// Post-process the existing report renderer: preserve layout, remove sections that overclaim coarse windows.
if(typeof renderReport==='function'){
 const oldRender=renderReport;
 renderReport=function(r){oldRender(r);
  const w=r.pacing?.wpm_verification,o=r.video?.outlier||{},tim=r.timing_precision||{};
  const facts=document.querySelector('#report .facts-card');if(facts&&w){const note=document.createElement('div');note.className='v10-note v10-good';note.innerHTML=`<strong>${esc(w.label||'Video WPM')} verified:</strong> ${esc(w.word_count)} deduped words ÷ ${esc((Number(w.duration_seconds||0)/60).toFixed(2))} video minutes = <b>${esc(w.calculated_wpm)}</b> WPM. ${w.verified?'Independent check matches.':'Check failed.'} Speaking-only WPM is not claimed.`;facts.appendChild(note)}
  if(facts&&tim.note){const note=document.createElement('div');note.className='v10-note v10-warn';note.innerHTML=`<strong>Timestamp quality:</strong> ${esc(tim.note)}`;facts.appendChild(note)}
  if(o.quality==='suspect'){document.querySelectorAll('#report .outlier').forEach(x=>{x.textContent='SUPPRESSED';x.classList.add('suppressed')});const oc=document.querySelector('#report .outlier-card');if(oc){const n=document.createElement('div');n.className='v10-note v10-warn';n.innerHTML=`<strong>Outlier sanity check failed:</strong> ${esc(o.quality_reason||'The sampled baseline looks broken.')} The raw multiple is not treated as a product fact.`;oc.appendChild(n)}}
  document.querySelectorAll('#report .card .eyebrow').forEach(e=>{const t=(e.textContent||'').toUpperCase();if(t.includes('ATTENTION TIMELINE')||t.includes('RISK FLAGS'))e.closest('.card')?.remove()});
  const eng=[...document.querySelectorAll('#report .card .eyebrow')].find(x=>(x.textContent||'').trim().toUpperCase()==='THE ENGINE')?.closest('.card');if(eng){const n=document.createElement('div');n.className='v10-note v10-warn';n.innerHTML='<strong>Preview heuristic:</strong> structure-engine detection still comes from deterministic transcript rules. Treat it as a hypothesis until the reasoning model is connected.';eng.appendChild(n)}
 };
}

// Safer Markdown: no fake cadence or old n-gram summary.
if(typeof reportMarkdown==='function'){
 reportMarkdown=function(r){const v=r.video||{},h=r.hook_framework||{},o=v.outlier||{},w=r.pacing?.wpm_verification||{},why=r.why_it_worked_specific||[],ks=r.content_extraction?.items||[],peaks=r.heatmap?.top_peaks||[],a=r.research_intelligence?.actionable_summary||{};return `# ${v.title||'YouTube video'}\n\n## Facts\n- Channel: ${v.channel||'-'}\n- Views: ${v.view_count??'-'}\n- Outlier: ${o.quality==='suspect'?'SUPPRESSED — suspect baseline':(o.multiple??'-')+'x'}\n- Channel median: ${o.channel_median_views??'-'}\n- Video WPM: ${w.calculated_wpm??r.pacing?.video_words_per_minute??'-'} (${w.verified?'verified':'not verified'})\n- Transcript timing: approximate; ≈ timestamps are interpolated from coarse source windows\n\n## Hook\nType: ${h.primary_type||'-'}\nLine: "${h.exact_line||'-'}"\n\n## Why it worked — with receipts\n${why.map(x=>`- **${x.label} ${x.timestamp||''}** — ${x.claim}\n  - "${x.example||''}"`).join('\n')}\n\n## Most Replayed\n${peaks.map((x,i)=>`${i+1}. ${time(x.time)} — ${x.value}`).join('\n')||'No public replay data.'}\n\n## Key takeaways\n${ks.map((x,i)=>`${i+1}. ${x.statement} (${x.timestamp||''})`).join('\n')||'No clean takeaways extracted.'}\n\n## Steal these mechanics\n${(a.transferable_mechanics||[]).map(x=>`- ${x}`).join('\n')}`};
}

function getPatternHistory(){try{return JSON.parse(localStorage.getItem(PATTERN_STORE)||'[]')}catch{return[]}}
function setPatternHistory(xs){try{localStorage.setItem(PATTERN_STORE,JSON.stringify(xs.slice(0,40)))}catch{}}
function savePattern(p){const h=getPatternHistory(),id='pm-'+Date.now(),first=p.stats?.[0]?.title||'Pattern Mine',row={id,created:Date.now(),name:first,video_count:p.video_count,markdown:p.markdown,repeated:(p.most_repeated||[]).slice(0,8),unique:(p.unique||[]).slice(0,8),winner:p.winner_vs_pack||{},hooks:p.hooks||[],stats:p.stats||[]};h.unshift(row);setPatternHistory(h);return row}
function renderPatternHistory(){const root=document.querySelector('#historyRoot'),xs=getPatternHistory();if(!root||!xs.length)return;const wrap=document.createElement('div');wrap.className='pattern-history';wrap.innerHTML=`<div class="section-head"><div><div class="eyebrow">PATTERN MINE HISTORY</div><h2>Saved cross-video research</h2></div><span class="source-note">${xs.length} saved mines</span></div><div class="card">${xs.map(x=>`<div class="pattern-saved"><div><h4>${esc(x.name||'Pattern Mine')}</h4><small>${new Date(x.created).toLocaleString()} · ${x.video_count} videos · ${(x.repeated||[]).length} repeated clusters</small></div><div class="actions"><button class="btn small" data-pm-copy="${esc(x.id)}">Copy MD</button><button class="btn small" data-pm-delete="${esc(x.id)}">Delete</button></div></div>`).join('')}</div>`;root.appendChild(wrap);wrap.querySelectorAll('[data-pm-copy]').forEach(b=>b.onclick=async()=>{const x=getPatternHistory().find(y=>y.id===b.dataset.pmCopy);if(x)try{await navigator.clipboard.writeText(x.markdown||'');b.textContent='Copied ✓'}catch{}});wrap.querySelectorAll('[data-pm-delete]').forEach(b=>b.onclick=()=>{setPatternHistory(getPatternHistory().filter(x=>x.id!==b.dataset.pmDelete));renderHistory()})}
if(typeof renderHistory==='function'){const oldHistory=renderHistory;renderHistory=function(){oldHistory();renderPatternHistory()}}
if(typeof renderFolder==='function'){const oldFolder=renderFolder;renderFolder=function(xs){if(xs?.length<3){const root=document.querySelector('#folderDetail');if(!root)return;root.innerHTML=`<div class="card"><div class="eyebrow">${esc(selectedNiche||'FOLDER')}</div><h2>More evidence needed</h2><div class="v10-note v10-warn"><strong>${xs.length}/3 videos.</strong> YTIntel will not call something a “winning pattern” from one or two examples anymore. Analyse at least 3 videos in this folder first.</div>${xs.map(x=>`<div class="video-row"><img class="thumb" src="${esc(x.thumbnail||'')}" alt=""><div class="grow"><h3>${esc(x.title||'Untitled')}</h3><div class="muted">${esc(x.channel||'')} · ${compact(x.views)} views · ${x.wpm!=null?Math.round(x.wpm)+' Video WPM':'WPM n/a'}</div></div></div>`).join('')}</div>`;return}return oldFolder(xs)}}

// Rebuild Pattern Mine renderer around the v0.10 semantic output and save it to History.
function sourceBlock(s){return `<div class="compact-source"><b>${esc(s.video_title||'Video')}</b> · ${esc(s.timestamp||'')}<br>“${esc(s.exact_line||'')}”</div>`}
window.renderPatternMine=function(p){if(!p){document.querySelector('#batchRoot').innerHTML='<div class="card empty">No semantic Pattern Mine result returned.</div>';return}const w=p.winner_vs_pack||{};document.querySelector('#batchRoot').innerHTML=`<div class="pattern-toolbar"><div><div class="eyebrow">SEMANTIC PATTERN MINE</div><h2>${p.video_count} videos · meaning-aware clusters</h2><span class="semantic-badge">gte-small embeddings · not noun overlap</span></div><button class="btn primary" id="copyPatternMd">Copy Markdown</button></div><div class="grid"><article class="card c12 winner-box"><div class="eyebrow">1 · MOST REPEATED</div><h2>What the majority actually says</h2><p class="muted">${esc(p.repeated_definition||'')}</p>${(p.most_repeated||[]).map(x=>`<div class="pattern-point"><div class="split"><b>${esc(x.statement)}</b><span class="pill">${x.video_count}/${p.video_count}</span></div>${(x.sources||[]).slice(0,4).map(sourceBlock).join('')}</div>`).join('')||'<p class="muted">No claim cleared the majority threshold.</p>'}</article><article class="card c12"><div class="eyebrow">2 · UNIQUE / UNCOMMON</div><h2>Where genuinely different material may exist</h2>${(p.unique||[]).slice(0,20).map(x=>`<div class="pattern-point"><b>${esc(x.statement)}</b>${(x.sources||[]).slice(0,1).map(sourceBlock).join('')}</div>`).join('')||'<p class="muted">No clean unique claims isolated.</p>'}</article><article class="card c12 winner-box"><div class="eyebrow">3 · WINNER VS PACK</div><h2>${esc(w.title||'Winner')}</h2><div class="metrics lean-metrics">${metric('Outlier',w.outlier_multiple==null?'-':num(w.outlier_multiple)+'×')}${metric('Winner WPM',num(w.wpm))}${metric('Pack WPM',num(w.pack_median_wpm))}${metric('Hook',w.hook_type||'-')}</div><blockquote>“${esc(w.hook_exact||'-')}”</blockquote><h3>Winner-only material</h3>${(w.winner_unique||[]).slice(0,12).map(x=>`<div class="pattern-point"><b>${esc(x.statement)}</b>${sourceBlock(x.sources?.[0]||{})}</div>`).join('')||'<p class="muted">No clean winner-only claims cleared the semantic rules.</p>'}</article><article class="card c12"><div class="eyebrow">4 · HOOKS SIDE BY SIDE</div>${(p.hooks||[]).map(h=>`<div class="pattern-point"><div class="split"><b>${esc(h.title||'Video')}</b><span class="pill">${esc(h.hook_type||'-')}</span></div><blockquote>“${esc(h.hook_line||'-')}”</blockquote></div>`).join('')}</article><article class="card c12"><div class="eyebrow">5 · SMALL STATS TABLE</div><div class="tablewrap"><table class="table"><thead><tr><th>#</th><th>Video</th><th>Views</th><th>Outlier</th><th>Video WPM</th><th>Top replay</th></tr></thead><tbody>${(p.stats||[]).map(s=>`<tr><td>${s.video_index}</td><td>${esc(s.title||'')}</td><td>${compact(s.views)}</td><td>${s.outlier_multiple==null?'—':num(s.outlier_multiple)+'×'}</td><td>${num(s.wpm)}</td><td>${s.top_replay==null?'—':time(s.top_replay)}</td></tr>`).join('')}</tbody></table></div></article></div><div class="v10-note v10-warn"><strong>Current intelligence ceiling:</strong> Pattern Mine now uses semantic embeddings, but a Claude/OpenAI-class reasoning model is not connected yet. It is meaning-aware clustering, not full editorial judgment.</div>`;document.querySelector('#copyPatternMd').onclick=async()=>{try{await navigator.clipboard.writeText(p.markdown||'');document.querySelector('#copyPatternMd').textContent='Copied ✓'}catch{}};savePattern(p)};
const batchBtn=document.querySelector('#batchBtn');if(batchBtn)batchBtn.onclick=async()=>{const urls=document.querySelector('#batchUrls').value.split(/\n+/).map(x=>x.trim()).filter(Boolean);if(urls.length<5||urls.length>10){showErr('#batchError',new Error('Pattern Mine is for 5–10 near-identical videos.'));return}clearErr('#batchError');document.querySelector('#batchRoot').innerHTML='';document.querySelector('#batchLoad').classList.add('show');try{const d=await api('batch',{urls,name:'Same-format semantic mine'});renderPatternMine(d.pattern_mine_v10);renderPatternHistory()}catch(e){showErr('#batchError',e)}finally{document.querySelector('#batchLoad').classList.remove('show')}};

// Honest Visual + Audio product state. No Content-ID metadata masquerading as an audio analyser.
window.renderVisuals=function(){const root=document.querySelector('#visualRoot');if(!root)return;const r=lastReport||null,v=r?.video||{},targets=r?.visual_intelligence?.hotzone_targets||[];root.innerHTML=`<div class="grid"><article class="card c12"><div class="eyebrow">CURRENT VIDEO</div><h2>${esc(v.title||'Analyse a video first')}</h2><p>${r?'Replay evidence can tell us where to inspect. It cannot tell us what visual/audio caused the peak until media analysis is live.':'Analyse a video first to load its replay targets.'}</p></article><article class="card c12"><div class="eyebrow">HOTZONE TARGETS · LIVE</div><h2>Where frame analysis should look first</h2>${targets.length?targets.slice(0,8).map((x,i)=>`<div class="visual-target"><div class="visual-rank">#${i+1}</div><div class="grow"><div class="split"><b>${esc(x.timestamp||time(x.time))}</b><span class="pill">replay ${num(x.replay_score)}</span></div><p>${esc(x.transcript||'Transcript alignment unavailable at this peak.')}</p><div class="source-note">Visual cause: not classified until frames are sampled.</div></div></div>`).join(''):'<p class="muted">No public Most Replayed targets for the current video.</p>'}</article><article class="card c12"><div class="audio-road"><div class="audio-card"><span class="state">NEXT · NO VISION MODEL NEEDED</span><h3>Cuts / Edit Pace</h3><p>Download/process the media and run scene-change detection to calculate actual cuts/min, shot-length distribution and edit-rate changes around hotzones.</p></div><div class="audio-card"><span class="state">PLANNED</span><h3>Sound Effects</h3><p>Separate speech/music/SFX first, then classify repeated impact, whoosh, riser, click, meme and transition sounds. Bank them by niche + outlier + hotzone strength.</p></div><div class="audio-card"><span class="state">PLANNED</span><h3>B-roll / Background Music</h3><p>Real audio fingerprinting/recognition is required. YouTube's Content-ID attribution box is not enough for gaming, tutorials or talking-head videos, so it is no longer presented as the vault.</p></div></div></article><article class="card c12"><div class="eyebrow">LONG-GAME VAULT</div><h2>What this becomes after 100s of videos</h2><div class="truth-grid"><div class="truth-box"><div class="eyebrow">VISUALS</div><b>Motion graphics / captions / zooms / B-roll</b><p>Rank recurring visual choices by niche, outlier performance and whether they occur near replay hotzones.</p></div><div class="truth-box"><div class="eyebrow">AUDIO</div><b>SFX + background tracks</b><p>Build a reusable bank of sounds/music actually detected in the media, not metadata guesses.</p></div><div class="truth-box"><div class="eyebrow">EDITING</div><b>Cuts/min + shot behaviour</b><p>Cheap scene detection across the whole video; expensive vision only around the strongest evidence windows.</p></div></div></article></div>`};

// Add retention truth to Similar Videos rather than inventing a percentage.
const similarHero=document.querySelector('#similar .hero');if(similarHero&&!similarHero.querySelector('.retention-truth')){const n=document.createElement('div');n.className='v10-note v10-warn retention-truth';n.innerHTML='<strong>Audience retention:</strong> “% still watching at 3:00” and “% stayed to the end” are private YouTube Analytics metrics. YTIntel can show them later for a creator’s own connected channel, but cannot truthfully show them for arbitrary competitor videos. Public Most Replayed is a different metric.';similarHero.appendChild(n)}

// Refresh the now-overridden tabs.
try{renderHistory();renderVisuals()}catch{}
})();