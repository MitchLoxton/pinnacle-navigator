(()=>{'use strict';
const $=s=>document.querySelector(s),clean=s=>String(s??'').replace(/\s+/g,' ').trim(),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const words=s=>(clean(s).toLowerCase().match(/[a-z0-9'$%’'-]+/g)||[]);
const STOP=new Set('the a an and or but if then than that this these those to of in on at for from with as is are was were be been being it its i me my we our you your he she they them their his her do does did doing have has had can could would should will just so very really also there here what when where why how who which into about over under up down out more most less few many much one two three four five six seven eight nine ten video videos youtube new get got going go make made see saw know think thing things people time now yeah okay right actually basically kind sort well gonna want need needs used using use way all every even still much many around through because cause maybe probably definitely good great'.split(' '));
function terms(s,n=10){const m=new Map;for(const w of words(s)){const k=w.replace(/[^a-z0-9$%]/g,'');if(k.length<4||STOP.has(k)||/^\d+$/.test(k))continue;m.set(k,(m.get(k)||0)+1)}return[...m.entries()].sort((a,b)=>b[1]-a[1]).slice(0,n).map(x=>x[0])}
function titleShape(t){const bits=[];if(/\bhow\b|\bhow to\b/i.test(t))bits.push('how-to');if(/\b(secret|hidden|nobody|untapped|cheatcode)\b/i.test(t))bits.push('curiosity / insider');if(/\b\d+[,.]?\d*|\$|%/.test(t))bits.push('number-led');if(/\bnew|just|today|2026|breaking|update/i.test(t))bits.push('recency');if(/[.]{2,}|\([^)]{2,}\)/.test(t))bits.push('house-style punctuation / bracket suffix');return bits.length?bits.join(' + '):'topic-led / curiosity-led'}
function promiseRead(title,opening,body){const T=terms(title,8),O=new Set(terms(opening,30)),B=new Set(terms(body,80)),open=T.filter(x=>O.has(x)),full=T.filter(x=>B.has(x));return{terms:T,open,full}}
function patch(){const r=window.__YTINTEL_NORMALIZED_REPORT,sec=$('#report[data-v363-premium="1"] [data-section="12"]');if(!r||!sec||sec.querySelector('#v369-package-depth'))return;const title=clean(r.video?.title),segs=r.transcript?.segments||[],opening=clean(segs.filter(x=>Number(x.start)<60).map(x=>x.text).join(' ')),body=clean(segs.map(x=>x.text).join(' ')),p=promiseRead(title,opening,body),desc=clean(r.video?.description),hasLinks=/https?:\/\//.test(desc),shape=titleShape(title),thumb=r.video?.id?`https://i.ytimg.com/vi/${r.video.id}/maxresdefault.jpg`:'';const copy=`<div id="v369-package-depth" class="v363-panel" style="margin-top:12px"><div class="eyebrow">PACKAGE MECHANICS — BENCHMARK DEPTH</div><h3>What the click promise is actually doing</h3><p><b>Title shape:</b> ${esc(shape)}. The title concentrates on <b>${esc((p.terms||[]).slice(0,6).join(', ')||'the source topic')}</b>. ${p.open.length?`The opening minute pays some of that promise immediately through ${esc(p.open.join(', '))}.`:`The first minute does not strongly repeat the title's core terms, so the report treats early promise fulfilment cautiously.`} Across the full transcript, ${p.full.length}/${Math.max(1,p.terms.length)} tracked title terms recur; that is evidence of topical delivery, not proof that the title was satisfying or causal to performance.</p><p><b>Thumbnail / title relationship:</b> ${thumb?`A public thumbnail URL exists for this upload. YTIntel keeps visual composition claims inside the separate verified visual pass; it does not invent facial expression, arrows, text or props from the URL alone.`:`A reliable public thumbnail was not available on this pull, so composition is withheld.`} The title test card below compares public title surfaces when the channel/RSS/search pull exposes them. Multiple surfaces are useful evidence of live title variation, but they can also reflect edits or cache lag; only creator-authorised experiment data proves an A/B test.</p><p><b>Commercial context:</b> ${hasLinks?`The description contains external links, so package-versus-delivery should be read alongside the funnel analysis in Channel Context. A strong click promise can be serving lead generation as much as raw watch-time goals.`:`No external description link was detected in the source field on this pull, so YTIntel does not infer a commercial funnel from packaging alone.`} For a remake, copy the promise architecture and evidence timing—not the competitor's exact title wording, thumbnail execution or unsupported authority cues.</p></div>`;sec.insertAdjacentHTML('beforeend',copy)}
function start(){window.addEventListener('ytintel:v363-premium-ready',patch);window.addEventListener('ytintel:v364-facts-ready',patch);new MutationObserver(()=>{if($('#report[data-v363-premium="1"]'))patch()}).observe(document.body,{childList:true,subtree:true});patch();window.YTIntelPackagingDepth={version:'0.37.0',patch}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();
