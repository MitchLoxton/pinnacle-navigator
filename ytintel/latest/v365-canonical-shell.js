(()=>{'use strict';
const STEPS=['The strip','The summary — so you never watch it','Key takeaways','The hook','The re-hooks','The payoffs','Most Replayed','The structure map','Visuals — key frame by key frame','Motion graphics','Audio','Packaging','The numbers table','The claims ledger','Channel context','Make it yours','The vault entry','Export'];
const $=s=>document.querySelector(s),clean=s=>String(s??'').replace(/\s+/g,' ').trim();let queued=false;
function patchHero(){const h=$('#analyse .hero');if(!h)return;const eye=h.querySelector('.eyebrow'),p=h.querySelector('p');if(eye)eye.textContent='YTINTEL ENDGAME · PREMIUM VIDEO ANALYSIS';if(p)p.textContent='One winning video in. An 18-section premium source-backed analysis out: watch-replacement summary, exact receipts, hooks, re-hooks, payoffs, Most Replayed, structure, visual/audio evidence, packaging, numbers, claims ledger, channel context, Creator-DNA remake, Vault entry and export.';const b=h.querySelector('.v360-media-banner');if(b){const strong=b.querySelector('strong'),copy=b.querySelector('p');if(strong)strong.textContent='Benchmark-depth Endgame engine';if(copy)copy.textContent='Source evidence always runs. The visible brief follows the 09/09 premium structure. On compatible desktop Chrome, optional on-device text + image + audio intelligence can deepen the model/media sections without paid API credits.';const badges=b.querySelectorAll('.v360-badge');if(badges[0])badges[0].textContent='18 sections';}}
function patchRoadmap(){const root=$('#v350-console .roadmap-grid');if(!root)return;const pct=parseInt($('#progressPct')?.textContent||'0',10)||0;const at=[6,15,22,29,35,41,47,54,60,65,70,75,80,85,90,94,97,100];const html=STEPS.map((title,i)=>{const done=pct>=at[i],running=!done&&(i===0||pct>=at[i-1]);return`<div class="roadstep ${done?'done':running?'running':'idle'}"><strong>${i+1}</strong><span>${title}</span><i>${done?'done':running?'working':'queued'}</i></div>`}).join('');if(root.dataset.v365Pct!==String(pct)||root.dataset.v365Count!=='18'){root.innerHTML=html;root.dataset.v365Pct=String(pct);root.dataset.v365Count='18'}}
function patch(){queued=false;patchHero();patchRoadmap();const state=$('#progressState');if(state&&clean(state.textContent)==='EVIDENCE COMPLETE'&&$('#report')?.dataset.v363Premium==='1')state.textContent='COMPLETE'}
function queue(){if(queued)return;queued=true;queueMicrotask(patch)}
function start(){new MutationObserver(queue).observe(document.body,{childList:true,subtree:true,characterData:true});patch();window.YTIntelCanonicalShell={version:'0.37.0',steps:STEPS}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();
