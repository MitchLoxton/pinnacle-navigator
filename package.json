{
  "name": "pinnacle-navigator",
  "private": true,
  "version": "51.2.0",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html manifest.webmanifest icon.svg sw.js v51.css v51.js foreman.js progress.js version.json dist/"
  }
}
