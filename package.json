{
  "name": "pinnacle-navigator",
  "private": true,
  "version": "51.1.0",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html manifest.webmanifest icon.svg sw.js v51.css v51.js foreman.js version.json dist/"
  }
}
