name: Progress HQ Smoke
on:
  push:
    paths:
      - 'ytintel/progress/**'
      - 'ytintel/latest/v401-progress-link.js'
      - 'ytintel/latest/v390-stable-shell.js'
      - '.github/workflows/progress-hq-smoke.yml'
  workflow_dispatch:
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Check Progress HQ JavaScript syntax
        run: |
          node --check ytintel/progress/app.js
          node --check ytintel/progress/shared.js
          node --check ytintel/latest/v401-progress-link.js
      - name: Verify shared gamified UI and YTIntel bridge
        run: node ytintel/progress/smoke.mjs