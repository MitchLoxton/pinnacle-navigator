{"version":65,"label":"v53.59","build":5366,"channel":"workshop","released":"2026-09-10","changes":["Fixed the desktop width jumping shown in the workshop screenshots","Replaced the dynamic visual layer with a static stability-first workshop skin that does not poll, observe, resize or repeatedly rewrite the DOM","The fixed layout stylesheet is loaded before the real task screen renders so the app no longer snaps between the legacy narrow width and the wider workshop width","Added scrollbar-gutter stability and a single fixed desktop workspace width to prevent horizontal reflow","Replaced the old stability runtime with stability-core.js: no crew interception, no MutationObserver, no layout loop and no repeated render timers","Retained local offline DONE queueing and cached reads for temporary connection failures","Morning Pack, 42.5 mm template, shared DONE controls, task state, assignments, holds and project scope remain intact","Started a clean v5 private workshop bundle cache; no shared project data was reset"]}
